query GetProjects($first: Int, $skip: Int) {
    projects(first: $first, skip: $skip) {
        nodes {
            id
            name
            description
            repository
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            totalCount
        }
        totalCount
    }
}
