query GetDefects($projectId: UUID!, $first: Int!) {
  defects(projectId: $projectId, first: $first) {
    nodes {
      id
      title
      status
      description
      acceptanceCriteria
      plan
      featureSubtype
      epicId
    }
  }
}
