query GetTasks($projectId: UUID!, $first: Int!) {
  tasks(projectId: $projectId, first: $first) {
    nodes {
      id
      title
      status
      deliverable
      acceptanceCriteria
      risks
      itemId
      projectId
      item {
        id
        title
        description
      }
    }
  }
}
