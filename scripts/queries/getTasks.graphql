query GetTasks($itemId: UUID!) {
    agentTasks(itemId: $itemId) {
        id
        title
        status
        deliverableId
        description
        result
        errors
        commitHash
        complexityRating
        dependsOnAgentTaskId
        promptTokens
        completionTokens
        executionDurationInSeconds
        agent
    }
}
