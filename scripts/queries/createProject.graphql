mutation CreateProject($name: String!, $description: String, $repository: String!) {
    createProject(input: { name: $name, description: $description, repository: $repository }) {
        project {
            id
            name
            description
            repository
        }
        errors
    }
}
