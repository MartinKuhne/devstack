query GetEpics($projectId: UUID, $first: Int!) {
  items(projectId: $projectId, subtype: [EPIC], first: $first) {
    nodes {
      id
      title
      description
      projectId
      status
      featureSubtype
      epicId
      createdAt
      updatedAt
    }
  }
}
