query GetEpics {
  items(subtype: [EPIC], first: 200) {
    nodes {
      id
      title
      description
      projectId
      createdAt
      updatedAt
      dependsOn {
        id
        status
      }
    }
  }
}
