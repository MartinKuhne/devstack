query GetProject($id: UUID!) {
    projectById(id: $id) {
        id
        name
        description
        architecture
        memory
        githubUrl
        createdAt
        updatedAt
    }
}
