query GetFeatures($projectId: ID, $status: [FeatureStatus!]) {
    features(projectId: $projectId, status: $status) {
        edges {
            node {
                id
                title
                status
                updatedAt
                tasks {
                    id
                }
            }
        }
    }
}
