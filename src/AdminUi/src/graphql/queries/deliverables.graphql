query GetDeliverables {
    deliverables {
        nodes {
            id
            title
            description
            status
            type
            acceptanceCriteria
            executionPlan
            agentFeedback
            securityImpact
            performanceImpact
            testPlan
            deploymentPlan
            blocking
        }
    }
}

query GetDeliverableById($id: UUID!) {
    deliverableById(id: $id) {
        id
        title
        description
        status
        type
        acceptanceCriteria
        executionPlan
        agentFeedback
        securityImpact
        performanceImpact
        testPlan
        deploymentPlan
        blocking
    }
}
