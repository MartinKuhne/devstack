query ModelConfigurations($projectId: UUID!) {
    modelConfigurations(projectId: $projectId) {
        id
        projectId
        url
        model
        modelAlias
        maxComplexity
        createdAt
        updatedAt
    }
}
