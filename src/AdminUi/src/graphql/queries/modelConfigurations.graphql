query ModelConfigurations {
    largeLanguageModels(first: 100) {
        nodes {
            id
            url
            model
            modelAlias
            apiKey
            cost
            maxComplexity
            maxConcurrency
        }
    }
}
