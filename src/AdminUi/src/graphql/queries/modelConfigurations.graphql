query ModelConfigurations($projectId: ID!) {
    modelConfigurations(projectId: $projectId) {
        id
        projectId
        url
        model
        modelAlias
        maxComplexity
        createdAt
        updatedAt
    }
}
