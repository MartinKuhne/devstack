mutation UpdateFeature($id: ID!, $input: UpdateFeatureInput!) {
    updateFeature(id: $id, input: $input) {
        id
        title
        description
        acceptanceCriteria
        plan
        securityImpact
        performanceImpact
        testPlan
        deploymentPlan
        openQuestions
        status
    }
}
