mutation UpdateDeliverable($input: UpdateDeliverableInput!) {
    updateDeliverable(input: $input) {
        deliverable {
            id
            title
            status
        }
        errors
    }
}
