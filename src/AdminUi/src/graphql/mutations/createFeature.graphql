mutation CreateFeature($input: CreateFeatureInput!) {
    createFeature(input: $input) {
        item {
            id
            title
            description
            acceptanceCriteria
            openQuestions
            status
            projectId
            createdAt
            updatedAt
        }
        errors
    }
}
