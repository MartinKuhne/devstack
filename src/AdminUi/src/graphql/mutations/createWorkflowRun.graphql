mutation CreateWorkflowRun($input: CreateWorkflowRunInput!) {
    createWorkflowRun(input: $input) {
        workflowRun {
            id
            projectId
            featureId
            taskId
            workflowType
            status
            startedAt
            createdAt
        }
        errors
    }
}
