mutation CreateTask($input: CreateTaskInput!) {
    createTask(input: $input) {
        id
        title
        deliverable
        acceptanceCriteria
        risks
        requiredFollowUps
        complexity
        status
    }
}
