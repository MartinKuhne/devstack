mutation CreateTask($input: CreateTaskInput!) {
    createTask(input: $input) {
        task {
            id
            title
            deliverable
            acceptanceCriteria
            risks
            requiredFollowUps
            complexityRating
            result
            status
            featureId
            createdAt
            updatedAt
        }
        errors
    }
}
