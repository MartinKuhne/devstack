mutation CreateProject($input: CreateProjectInput!) {
    createProject(input: $input) {
        project {
            id
            name
            description
            architecture
            memory
            githubUrl
            createdAt
            updatedAt
        }
        errors
    }
}
