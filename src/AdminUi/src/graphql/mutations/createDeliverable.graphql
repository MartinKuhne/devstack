mutation CreateDeliverable($input: CreateDeliverableInput!) {
    createDeliverable(input: $input) {
        deliverable {
            id
            title
            status
        }
        errors {
            field
            message
        }
    }
}
