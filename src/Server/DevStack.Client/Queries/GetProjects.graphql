query GetProjects($first: Int!, $repository: String) {
  projects(first: $first, where: { repository: { eq: $repository } }) {
    nodes {
      id
      name
      repository
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
