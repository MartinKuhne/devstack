query GetProjects($first: Int, $skip: Int) {
  projects(first: $first, skip: $skip) {
    nodes {
      id
      name
      repository
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
    totalCount
  }
}
