query GetEpics($title: String, $first: Int, $skip: Int) {
  getEpics(title: $title, first: $first, skip: $skip) {
    nodes {
      id
      title
      description
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
    totalCount
  }
}